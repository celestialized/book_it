


<div class="row">
  <div class="col-sm-6">
    <h1>Consultations</h1>
  </div>

  <div class="col-sm-6 text-right">
  <%= link_to new_consultation_path, class: 'btn btn-primary' do %>
    Add New Consultation
  <% end %>
  </div>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
    

            <th>Title</th>
            <th>Duration in minutes</th>
            <th>Cost in pennies</th>
            <th>Category</th>
            <th>Level</th>
            <th>Description</th>
            <th>Number of sessions</th>
            <th>Sku</th>
            
          <th></th>
        
      </tr>
    </thead>

    <tbody>
      <% @consultations.each do |consultation| %>
        <%= content_tag :tr, id: dom_id(consultation), class: dom_class(consultation) do %>
          

                      <td><%= consultation.title %></td>
                      <td><%= consultation.duration_in_minutes %></td>
                      <td><%= consultation.cost_in_pennies %></td>
                      <td><%= consultation.category %></td>
                      <td><%= consultation.level %></td>
                      <td><%= consultation.description %></td>
                      <td><%= consultation.number_of_sessions %></td>
                      <td><%= consultation.sku %></td>
          
          
            <td><%= link_to 'Show', consultation %></td>
          
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>
